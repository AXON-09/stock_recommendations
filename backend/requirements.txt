fastapi>=0.111.0
uvicorn[standard]>=0.30.0
yfinance>=0.2.40
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
xgboost>=2.0.0
requests>=2.31.0
# ta is kept for compatibility but indicators are computed manually
# PyTorch — install separately if/when a wheel for your Python version exists:
# pip install torch  (requires Python <= 3.12 for stable wheels as of 2026)
pytest>=8.0.0
